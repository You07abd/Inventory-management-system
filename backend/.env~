# Local development database (matches docker-compose.yml)
# Run: docker compose up -d   then: alembic upgrade head   then: python seed.py
DATABASE_URL=postgresql+psycopg2://inv_user:inv_pass@localhost:5432/drone_inventory
FRONTEND_ORIGIN=http://localhost:5173
