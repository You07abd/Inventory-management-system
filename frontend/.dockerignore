# Secrets — never bake into image layers
.env
.env.*
!.env.example

# Dependencies / build output (rebuilt inside the image)
node_modules/
dist/

# Local tooling
.claude/
.vscode/

# VCS
.git/
.gitignore
